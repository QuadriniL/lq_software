import 'package:flutter/material.dart';

class CreateClientDesktopPage extends StatelessWidget {
  const CreateClientDesktopPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Text('CreateClientDesktopPage'),
    );
  }
}
